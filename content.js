// // Content Script Logic
// // This script runs in the context of web pages when the extension is active

// // Example: Modify the page DOM or interact with the page

// // Example: Listen for events on the page and perform actions
// document.addEventListener('DOMContentLoaded', function () {
//     // Code to execute when the DOM content is loaded on the page
//   });
  
//   // Example: Send a message to the background script
//   chrome.runtime.sendMessage({ message: 'Hello from content script!' }, function (response) {
//     // Code to handle the response from the background script
//   });
  
//   // Example: Receive messages from the background script
//   chrome.runtime.onMessage.addListener(function (message, sender, sendResponse) {
//     // Code to handle the received message from the background script
  
//     // Example: Send a response back to the background script
//     sendResponse('Response from content script!');
//   });
  